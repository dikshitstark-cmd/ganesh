services:
  - type: web
    name: ganesh-utsav-receipts
    env: python
    buildCommand: "pip install -r requirements.txt && python generate_placeholders.py"
    startCommand: "gunicorn -w 4 -b 0.0.0.0:$PORT app:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        value: "akhil1@A"
      - key: FORCE_SECURE_COOKIES
        value: "1"
