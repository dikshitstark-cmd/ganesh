# Copy this file to .env for local use with a tool like python-dotenv,
# or set these as real environment variables on your hosting platform.
# None are required for local dev -- sensible defaults are used -- but
# SECRET_KEY and ADMIN_PASSWORD should always be set for a live deployment.

SECRET_KEY=replace-with-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
# DATABASE_PATH=/data/receipts.db
# PORT=5000
